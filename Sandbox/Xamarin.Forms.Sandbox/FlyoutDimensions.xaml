<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Xamarin.Forms.Sandbox.FlyoutDimensions"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:Xamarin.Forms.Sandbox"
    xmlns:xct="http://xamarin.com/schemas/2020/toolkit">

    <ContentPage.BindingContext>
        <vm:FlyoutDimensionsViewModel />
    </ContentPage.BindingContext>

    <StackLayout VerticalOptions="CenterAndExpand">
        <Entry
            x:Name="height"
            Keyboard="Numeric"
            Placeholder="Height">
            <Entry.Behaviors>
                <xct:EventToCommandBehavior
                    Command="{Binding HeightCommand}"
                    CommandParameter="{Binding Source={x:Reference height}, Path=Text}"
                    EventName="TextChanged" />
            </Entry.Behaviors>
        </Entry>
        <Entry
            x:Name="width"
            Keyboard="Numeric"
            Placeholder="Width">
            <Entry.Behaviors>
                <xct:EventToCommandBehavior
                    Command="{Binding WidthCommand}"
                    CommandParameter="{Binding Source={x:Reference width}, Path=Text}"
                    EventName="TextChanged" />
            </Entry.Behaviors>
        </Entry>
        <Button Command="{Binding DefaultCommand}" Text="Resetar valores" />
    </StackLayout>
</ContentPage>